Password is transfered during friend request, this could be insecure.
Implement a second authorisation step after both clients are connected.

Make data_send, data_recv in client.py more stable.

Better Timeout solution?
